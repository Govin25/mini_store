<h1>Edit User</h1>

<% if current_user.supplier? || current_user.id == @user.id %>
  <%= render "form", user: @user %>
<% else %>
  <p>You are not authorized to edit this user.</p>
<% end %>

<%= link_to "Back", users_path %>